{
  "name": "treatjar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A themed reward jar for kids — earn treats, watch the tokens pile up.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json --fail-on-warnings",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "matter-js": "^0.20.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@types/matter-js": "^0.20.0",
    "@types/node": "^26.2.0",
    "svelte": "^5.56.10",
    "svelte-check": "^4.3.4",
    "typescript": "^5.9.3",
    "vite": "^8.2.1",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.10"
  }
}
